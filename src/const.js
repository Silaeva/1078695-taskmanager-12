const COLORS = [
  `black`,
  `yellow`,
  `blue`,
  `green`,
  `pink`];

const MAX_DAYS_GAP = 7;

const TASK_COUNT = 22;

const SortType = {
  DEFAULT: `default`,
  DATE_DOWN: `date-down`,
  DATE_UP: `date-up`
};

export {COLORS, MAX_DAYS_GAP, TASK_COUNT, SortType};
