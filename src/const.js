const COLORS = [
  `black`,
  `yellow`,
  `blue`,
  `green`,
  `pink`];

const MAX_DAYS_GAP = 7;

const TASK_COUNT = 22;
const TASK_COUNT_PER_STEP = 8;

const RenderPosition = {
  AFTERBEGIN: `afterbegin`,
  BEFOREEND: `beforeend`
};

export {COLORS, MAX_DAYS_GAP, TASK_COUNT, TASK_COUNT_PER_STEP, RenderPosition};
